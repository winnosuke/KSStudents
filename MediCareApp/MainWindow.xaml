<Window x:Class="MediCareApp.MainWindow"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediCareApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="900" Background="Transparent" WindowStartupLocation="CenterScreen" AllowsTransparency="True" WindowStyle="None" >
    <Grid>
        <Border CornerRadius="18" Margin="18" Effect="{DynamicResource DropShadow}">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#fff" Offset="0"/>
                    <GradientStop Color="#fff" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header with app title, welcome and logout -->
                <DockPanel Grid.Row="0" Margin="16,16,16,8">
                    <TextBlock Text="MediCare Hospital Management System" FontSize="20" FontWeight="Bold" DockPanel.Dock="Left"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                        <TextBlock x:Name="txtWelcome" Margin="0,0,16,0" VerticalAlignment="Center"/>
                        <Button x:Name="btnLogout" Content="Logout" Width="90" Height="28" Click="BtnLogout_Click"/>
                    </StackPanel>
                </DockPanel>

                <!-- Các nút chức năng căn giữa -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Margin="0,24,0,24">
                    <Button Content="Patient Management" Width="200" Height="50" Margin="12" FontSize="18" Click="Menu_Grades_Click"/>
                    <Button Content="My Appointments" Width="220" Height="50" Margin="12" FontSize="18" Click="Menu_Students_Click"/>
                </StackPanel>

                <Border Grid.Row="2" Margin="24" Padding="18">
                    <ContentControl x:Name="MainContent" />
                </Border>
            </Grid>
        </Border>
    </Grid>
    <Window.Resources>
        <DropShadowEffect x:Key="DropShadow" BlurRadius="18" ShadowDepth="0" Color="#fff"/>
    </Window.Resources>
</Window>
